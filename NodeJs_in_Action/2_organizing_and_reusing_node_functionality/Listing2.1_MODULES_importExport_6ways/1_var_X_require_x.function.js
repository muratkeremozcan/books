var currency = require('./1_exports.function'); // module within same directory
// the way to import is the same in  export varieties
console.log( currency.canadianToUS(50));
console.log( currency.usToCanadian(30));
