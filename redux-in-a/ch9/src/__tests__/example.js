describe('a generic example', () => {
  it('should demonstrate basic syntax', () => {
    expect(5 + 1).toEqual(6);
  });

  it('should also demonstrate inverse assertions', () => {
    expect(true).not.toBe(false);
  });
});
