{
  "name": "parsnip",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "husky": "^0.13.4",
    "lint-staged": "^3.5.1",
    "prettier": "^1.3.1"
  },
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*/src/**/*.js": [
      "prettier --single-quote --trailing-comma es5 --write",
      "git add"
    ]
  }
}
